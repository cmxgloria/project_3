// print random color from a list

let colors = ["mistyrose", "pink", "plum"];

// fs is built in module
// modules
// _ same as , variable can lodash, should convention
const _ = require("lodash");

console.log(_.sample(colors));
